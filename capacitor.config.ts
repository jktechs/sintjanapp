import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'nl.sintjan.app',
  appName: 'sintjanapp',
  webDir: 'dist/sintjanapp',
  bundledWebRuntime: false
};

export default config;
